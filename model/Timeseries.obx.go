// Code generated by ObjectBox; DO NOT EDIT.
// Learn more about defining entities and generating this file - visit https://golang.objectbox.io/entity-annotations

package model

import (
	"errors"
	"github.com/google/flatbuffers/go"
	"github.com/objectbox/objectbox-go/objectbox"
	"github.com/objectbox/objectbox-go/objectbox/fbutils"
)

type timeseries_EntityInfo struct {
	objectbox.Entity
	Uid uint64
}

var TimeseriesBinding = timeseries_EntityInfo{
	Entity: objectbox.Entity{
		Id: 1,
	},
	Uid: 8153158015204459380,
}

// Timeseries_ contains type-based Property helpers to facilitate some common operations such as Queries.
var Timeseries_ = struct {
	Id                    *objectbox.PropertyUint64
	Did                   *objectbox.PropertyString
	Pid                   *objectbox.PropertyString
	DateCreated           *objectbox.PropertyInt64
	DateFinished          *objectbox.PropertyInt64
	temperature           *objectbox.PropertyInt64
	ph                    *objectbox.PropertyInt64
	orp                   *objectbox.PropertyInt64
	conductivity          *objectbox.PropertyInt64
	isc                   *objectbox.PropertyInt64
	dissolved_oxygen      *objectbox.PropertyInt64
	percent_concentration *objectbox.PropertyInt64
	custom_dcloop         *objectbox.PropertyInt64
}{
	Id: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     1,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	Did: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     2,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	Pid: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     3,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	DateCreated: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     4,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	DateFinished: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     5,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	temperature: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     6,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	ph: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     7,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	orp: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     8,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	conductivity: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     9,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	isc: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     10,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	dissolved_oxygen: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     11,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	percent_concentration: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     12,
			Entity: &TimeseriesBinding.Entity,
		},
	},
	custom_dcloop: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     13,
			Entity: &TimeseriesBinding.Entity,
		},
	},
}

// GeneratorVersion is called by ObjectBox to verify the compatibility of the generator used to generate this code
func (timeseries_EntityInfo) GeneratorVersion() int {
	return 6
}

// AddToModel is called by ObjectBox during model build
func (timeseries_EntityInfo) AddToModel(model *objectbox.Model) {
	model.Entity("Timeseries", 1, 8153158015204459380)
	model.Property("Id", 6, 1, 2509703330892030507)
	model.PropertyFlags(1)
	model.Property("Did", 9, 2, 7812185321541309883)
	model.Property("Pid", 9, 3, 7798639140585942360)
	model.Property("DateCreated", 6, 4, 230894173945468627)
	model.Property("DateFinished", 6, 5, 897216642820117754)
	model.Property("temperature", 6, 6, 2177781470075130656)
	model.Property("ph", 6, 7, 3490182989431628820)
	model.Property("orp", 6, 8, 3422570477071045128)
	model.Property("conductivity", 6, 9, 4208334973405299631)
	model.Property("isc", 6, 10, 2405307547551869550)
	model.Property("dissolved_oxygen", 6, 11, 1095142679241899690)
	model.Property("percent_concentration", 6, 12, 2844041708752432010)
	model.Property("custom_dcloop", 6, 13, 4432820829422156182)
	model.EntityLastPropertyId(13, 4432820829422156182)
}

// GetId is called by ObjectBox during Put operations to check for existing ID on an object
func (timeseries_EntityInfo) GetId(object interface{}) (uint64, error) {
	return uint64(object.(*Timeseries).Id), nil
}

// SetId is called by ObjectBox during Put to update an ID on an object that has just been inserted
func (timeseries_EntityInfo) SetId(object interface{}, id uint64) error {
	object.(*Timeseries).Id = int64(id)
	return nil
}

// PutRelated is called by ObjectBox to put related entities before the object itself is flattened and put
func (timeseries_EntityInfo) PutRelated(ob *objectbox.ObjectBox, object interface{}, id uint64) error {
	return nil
}

// Flatten is called by ObjectBox to transform an object to a FlatBuffer
func (timeseries_EntityInfo) Flatten(object interface{}, fbb *flatbuffers.Builder, id uint64) error {
	obj := object.(*Timeseries)
	var offsetDid = fbutils.CreateStringOffset(fbb, obj.Did)
	var offsetPid = fbutils.CreateStringOffset(fbb, obj.Pid)

	// build the FlatBuffers object
	fbb.StartObject(13)
	fbutils.SetUint64Slot(fbb, 0, id)
	fbutils.SetUOffsetTSlot(fbb, 1, offsetDid)
	fbutils.SetUOffsetTSlot(fbb, 2, offsetPid)
	fbutils.SetInt64Slot(fbb, 3, obj.DateCreated)
	fbutils.SetInt64Slot(fbb, 4, obj.DateFinished)
	fbutils.SetInt64Slot(fbb, 5, obj.temperature)
	fbutils.SetInt64Slot(fbb, 6, obj.ph)
	fbutils.SetInt64Slot(fbb, 7, obj.orp)
	fbutils.SetInt64Slot(fbb, 8, obj.conductivity)
	fbutils.SetInt64Slot(fbb, 9, obj.isc)
	fbutils.SetInt64Slot(fbb, 10, obj.dissolved_oxygen)
	fbutils.SetInt64Slot(fbb, 11, obj.percent_concentration)
	fbutils.SetInt64Slot(fbb, 12, obj.custom_dcloop)
	return nil
}

// Load is called by ObjectBox to load an object from a FlatBuffer
func (timeseries_EntityInfo) Load(ob *objectbox.ObjectBox, bytes []byte) (interface{}, error) {
	if len(bytes) == 0 { // sanity check, should "never" happen
		return nil, errors.New("can't deserialize an object of type 'Timeseries' - no data received")
	}

	var table = &flatbuffers.Table{
		Bytes: bytes,
		Pos:   flatbuffers.GetUOffsetT(bytes),
	}

	var propId = table.GetInt64Slot(4, 0)

	return &Timeseries{
		Id:                    propId,
		Did:                   fbutils.GetStringSlot(table, 6),
		Pid:                   fbutils.GetStringSlot(table, 8),
		DateCreated:           fbutils.GetInt64Slot(table, 10),
		DateFinished:          fbutils.GetInt64Slot(table, 12),
		temperature:           fbutils.GetInt64Slot(table, 14),
		ph:                    fbutils.GetInt64Slot(table, 16),
		orp:                   fbutils.GetInt64Slot(table, 18),
		conductivity:          fbutils.GetInt64Slot(table, 20),
		isc:                   fbutils.GetInt64Slot(table, 22),
		dissolved_oxygen:      fbutils.GetInt64Slot(table, 24),
		percent_concentration: fbutils.GetInt64Slot(table, 26),
		custom_dcloop:         fbutils.GetInt64Slot(table, 28),
	}, nil
}

// MakeSlice is called by ObjectBox to construct a new slice to hold the read objects
func (timeseries_EntityInfo) MakeSlice(capacity int) interface{} {
	return make([]*Timeseries, 0, capacity)
}

// AppendToSlice is called by ObjectBox to fill the slice of the read objects
func (timeseries_EntityInfo) AppendToSlice(slice interface{}, object interface{}) interface{} {
	if object == nil {
		return append(slice.([]*Timeseries), nil)
	}
	return append(slice.([]*Timeseries), object.(*Timeseries))
}

// Box provides CRUD access to Timeseries objects
type TimeseriesBox struct {
	*objectbox.Box
}

// BoxForTimeseries opens a box of Timeseries objects
func BoxForTimeseries(ob *objectbox.ObjectBox) *TimeseriesBox {
	return &TimeseriesBox{
		Box: ob.InternalBox(1),
	}
}

// Put synchronously inserts/updates a single object.
// In case the Id is not specified, it would be assigned automatically (auto-increment).
// When inserting, the Timeseries.Id property on the passed object will be assigned the new ID as well.
func (box *TimeseriesBox) Put(object *Timeseries) (uint64, error) {
	return box.Box.Put(object)
}

// Insert synchronously inserts a single object. As opposed to Put, Insert will fail if given an ID that already exists.
// In case the Id is not specified, it would be assigned automatically (auto-increment).
// When inserting, the Timeseries.Id property on the passed object will be assigned the new ID as well.
func (box *TimeseriesBox) Insert(object *Timeseries) (uint64, error) {
	return box.Box.Insert(object)
}

// Update synchronously updates a single object.
// As opposed to Put, Update will fail if an object with the same ID is not found in the database.
func (box *TimeseriesBox) Update(object *Timeseries) error {
	return box.Box.Update(object)
}

// PutAsync asynchronously inserts/updates a single object.
// Deprecated: use box.Async().Put() instead
func (box *TimeseriesBox) PutAsync(object *Timeseries) (uint64, error) {
	return box.Box.PutAsync(object)
}

// PutMany inserts multiple objects in single transaction.
// In case Ids are not set on the objects, they would be assigned automatically (auto-increment).
//
// Returns: IDs of the put objects (in the same order).
// When inserting, the Timeseries.Id property on the objects in the slice will be assigned the new IDs as well.
//
// Note: In case an error occurs during the transaction, some of the objects may already have the Timeseries.Id assigned
// even though the transaction has been rolled back and the objects are not stored under those IDs.
//
// Note: The slice may be empty or even nil; in both cases, an empty IDs slice and no error is returned.
func (box *TimeseriesBox) PutMany(objects []*Timeseries) ([]uint64, error) {
	return box.Box.PutMany(objects)
}

// Get reads a single object.
//
// Returns nil (and no error) in case the object with the given ID doesn't exist.
func (box *TimeseriesBox) Get(id uint64) (*Timeseries, error) {
	object, err := box.Box.Get(id)
	if err != nil {
		return nil, err
	} else if object == nil {
		return nil, nil
	}
	return object.(*Timeseries), nil
}

// GetMany reads multiple objects at once.
// If any of the objects doesn't exist, its position in the return slice is nil
func (box *TimeseriesBox) GetMany(ids ...uint64) ([]*Timeseries, error) {
	objects, err := box.Box.GetMany(ids...)
	if err != nil {
		return nil, err
	}
	return objects.([]*Timeseries), nil
}

// GetManyExisting reads multiple objects at once, skipping those that do not exist.
func (box *TimeseriesBox) GetManyExisting(ids ...uint64) ([]*Timeseries, error) {
	objects, err := box.Box.GetManyExisting(ids...)
	if err != nil {
		return nil, err
	}
	return objects.([]*Timeseries), nil
}

// GetAll reads all stored objects
func (box *TimeseriesBox) GetAll() ([]*Timeseries, error) {
	objects, err := box.Box.GetAll()
	if err != nil {
		return nil, err
	}
	return objects.([]*Timeseries), nil
}

// Remove deletes a single object
func (box *TimeseriesBox) Remove(object *Timeseries) error {
	return box.Box.Remove(object)
}

// RemoveMany deletes multiple objects at once.
// Returns the number of deleted object or error on failure.
// Note that this method will not fail if an object is not found (e.g. already removed).
// In case you need to strictly check whether all of the objects exist before removing them,
// you can execute multiple box.Contains() and box.Remove() inside a single write transaction.
func (box *TimeseriesBox) RemoveMany(objects ...*Timeseries) (uint64, error) {
	var ids = make([]uint64, len(objects))
	for k, object := range objects {
		ids[k] = uint64(object.Id)
	}
	return box.Box.RemoveIds(ids...)
}

// Creates a query with the given conditions. Use the fields of the Timeseries_ struct to create conditions.
// Keep the *TimeseriesQuery if you intend to execute the query multiple times.
// Note: this function panics if you try to create illegal queries; e.g. use properties of an alien type.
// This is typically a programming error. Use QueryOrError instead if you want the explicit error check.
func (box *TimeseriesBox) Query(conditions ...objectbox.Condition) *TimeseriesQuery {
	return &TimeseriesQuery{
		box.Box.Query(conditions...),
	}
}

// Creates a query with the given conditions. Use the fields of the Timeseries_ struct to create conditions.
// Keep the *TimeseriesQuery if you intend to execute the query multiple times.
func (box *TimeseriesBox) QueryOrError(conditions ...objectbox.Condition) (*TimeseriesQuery, error) {
	if query, err := box.Box.QueryOrError(conditions...); err != nil {
		return nil, err
	} else {
		return &TimeseriesQuery{query}, nil
	}
}

// Async provides access to the default Async Box for asynchronous operations. See TimeseriesAsyncBox for more information.
func (box *TimeseriesBox) Async() *TimeseriesAsyncBox {
	return &TimeseriesAsyncBox{AsyncBox: box.Box.Async()}
}

// TimeseriesAsyncBox provides asynchronous operations on Timeseries objects.
//
// Asynchronous operations are executed on a separate internal thread for better performance.
//
// There are two main use cases:
//
// 1) "execute & forget:" you gain faster put/remove operations as you don't have to wait for the transaction to finish.
//
// 2) Many small transactions: if your write load is typically a lot of individual puts that happen in parallel,
// this will merge small transactions into bigger ones. This results in a significant gain in overall throughput.
//
// In situations with (extremely) high async load, an async method may be throttled (~1ms) or delayed up to 1 second.
// In the unlikely event that the object could still not be enqueued (full queue), an error will be returned.
//
// Note that async methods do not give you hard durability guarantees like the synchronous Box provides.
// There is a small time window in which the data may not have been committed durably yet.
type TimeseriesAsyncBox struct {
	*objectbox.AsyncBox
}

// AsyncBoxForTimeseries creates a new async box with the given operation timeout in case an async queue is full.
// The returned struct must be freed explicitly using the Close() method.
// It's usually preferable to use TimeseriesBox::Async() which takes care of resource management and doesn't require closing.
func AsyncBoxForTimeseries(ob *objectbox.ObjectBox, timeoutMs uint64) *TimeseriesAsyncBox {
	var async, err = objectbox.NewAsyncBox(ob, 1, timeoutMs)
	if err != nil {
		panic("Could not create async box for entity ID 1: %s" + err.Error())
	}
	return &TimeseriesAsyncBox{AsyncBox: async}
}

// Put inserts/updates a single object asynchronously.
// When inserting a new object, the Id property on the passed object will be assigned the new ID the entity would hold
// if the insert is ultimately successful. The newly assigned ID may not become valid if the insert fails.
func (asyncBox *TimeseriesAsyncBox) Put(object *Timeseries) (uint64, error) {
	return asyncBox.AsyncBox.Put(object)
}

// Insert a single object asynchronously.
// The Id property on the passed object will be assigned the new ID the entity would hold if the insert is ultimately
// successful. The newly assigned ID may not become valid if the insert fails.
// Fails silently if an object with the same ID already exists (this error is not returned).
func (asyncBox *TimeseriesAsyncBox) Insert(object *Timeseries) (id uint64, err error) {
	return asyncBox.AsyncBox.Insert(object)
}

// Update a single object asynchronously.
// The object must already exists or the update fails silently (without an error returned).
func (asyncBox *TimeseriesAsyncBox) Update(object *Timeseries) error {
	return asyncBox.AsyncBox.Update(object)
}

// Remove deletes a single object asynchronously.
func (asyncBox *TimeseriesAsyncBox) Remove(object *Timeseries) error {
	return asyncBox.AsyncBox.Remove(object)
}

// Query provides a way to search stored objects
//
// For example, you can find all Timeseries which Id is either 42 or 47:
// 		box.Query(Timeseries_.Id.In(42, 47)).Find()
type TimeseriesQuery struct {
	*objectbox.Query
}

// Find returns all objects matching the query
func (query *TimeseriesQuery) Find() ([]*Timeseries, error) {
	objects, err := query.Query.Find()
	if err != nil {
		return nil, err
	}
	return objects.([]*Timeseries), nil
}

// Offset defines the index of the first object to process (how many objects to skip)
func (query *TimeseriesQuery) Offset(offset uint64) *TimeseriesQuery {
	query.Query.Offset(offset)
	return query
}

// Limit sets the number of elements to process by the query
func (query *TimeseriesQuery) Limit(limit uint64) *TimeseriesQuery {
	query.Query.Limit(limit)
	return query
}
